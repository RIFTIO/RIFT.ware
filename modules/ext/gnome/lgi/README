# Dynamic Lua binding to GObject libraries using GObject-Introspection
# lgi imported/tracked directly from the 
# upstream repository. The following commands are used to create this from 
# the upstream repo


# If fetching based on branch
git subtree add --prefix=lgi/lgi -m'Add the lgi master branch' --squash https://github.com/pavouk/lgi master

# Pull from the master
git subtree pull --prefix=lgi/lgi -m'merge lgi master' --squash https://github.com/pavouk/lgi master
